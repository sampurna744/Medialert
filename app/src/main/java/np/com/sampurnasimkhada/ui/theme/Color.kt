package np.com.sampurnasimkhada.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────
val Primary        = Color(0xFF6C63FF)   // violet – main brand
val PrimaryDark    = Color(0xFF3A33AA)
val PrimaryLight   = Color(0xFFEBEAFF)

val Secondary      = Color(0xFF00D9A6)   // teal – taken / success
val SecondaryDark  = Color(0xFF007A60)

val Danger         = Color(0xFFFF6B6B)   // coral – missed / delete
val DangerLight    = Color(0xFFFFECEC)

val Warning        = Color(0xFFFFB347)   // amber – warnings / gold
val WarningLight   = Color(0xFFFFF3E0)

val Info           = Color(0xFF4FC3F7)   // sky – informational

// ── Dark-theme surface palette ─────────────────────────────
val BgDeep         = Color(0xFF0D0D22)
val BgSurface      = Color(0xFF1A1A35)
val BgCard         = Color(0xFF16162E)
val BgInset        = Color(0xFF111127)
val BorderFaint    = Color(0xFF252545)

val TextPrimary    = Color(0xFFEEEEFF)
val TextSecondary  = Color(0xFFCCCCEE)
val TextMuted      = Color(0xFF5A5A8A)

// ── Light-theme surface palette ────────────────────────────
val LightBg        = Color(0xFFF7F5FF)
val LightSurface   = Color(0xFFFFFFFF)
val LightBorder    = Color(0xFFE8E4F5)
val LightMuted     = Color(0xFF9994B8)

// ── Pill colour options (user-selectable) ──────────────────
val PillColors = listOf(
    Color(0xFFFF6B6B),
    Color(0xFFFFB347),
    Color(0xFF00D9A6),
    Color(0xFF6C63FF),
    Color(0xFFFF8CC8),
    Color(0xFF4FC3F7),
    Color(0xFFA5D6A7),
    Color(0xFFCE93D8),
)
